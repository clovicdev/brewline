# Contributing

Keep changes small, test them with Gradle, and write code that fits the existing style.

Before opening a pull request:

- Run `./gradlew clean test shadowJar`.
- Keep command names aligned with `/brewline`.
- Update README notes when behavior changes.
- Keep GPLv3 notices intact.
